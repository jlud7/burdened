{
  "name": "burdened",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "playtest": "node scripts/playtest.ts",
    "art": "node scripts/generate-art.mjs",
    "art:bg": "node scripts/remove-backgrounds.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
